/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    trailingSlash: true,
    skipTrailingSlashRedirect: true,
    distDir: '/docs',
    basePath: '/portfolio',
};

export default nextConfig;
